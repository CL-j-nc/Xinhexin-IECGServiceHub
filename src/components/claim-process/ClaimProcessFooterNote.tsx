import React from 'react';

const ClaimProcessFooterNote: React.FC = () => {
    return (
        <div className="text-xs text-slate-400 text-center py-6">
            理赔处理具有阶段性，如有变更请以本系统状态为准。
        </div>
    );
};

export default ClaimProcessFooterNote;
